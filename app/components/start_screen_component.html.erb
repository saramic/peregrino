<div class="min-h-screen bg-canvas flex flex-col items-center justify-center gap-10 px-8">
  <div class="text-center">
    <h1 class="font-serif text-5xl text-prose tracking-tight" data-testid="wordmark">Peregrino</h1>
    <p class="mt-3 text-muted text-sm tracking-widest uppercase">Every road is a pilgrimage</p>
  </div>

  <div data-testid="start-button">
    <%= render ActionButtonComponent.new(label: "Start") %>
  </div>
</div>
